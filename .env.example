# OAuth2 credentials - use ONE of these methods:
# Option 1: JSON string in environment variable (useful for Docker/CI/CD)
# CLIENT_SECRET_JSON={"installed":{"client_id":"...","client_secret":"...","redirect_uris":["http://localhost"]}}

# Option 2: File path (default method)
CREDENTIALS_FILE=client_secret.json

# Playlist IDs (get these from YouTube playlist URLs)
TODO_PLAYLIST_ID=PLxxxxxxxxxxxxxxxxxxxxxxxxxxxx
DONE_PLAYLIST_ID=PLxxxxxxxxxxxxxxxxxxxxxxxxxxxx
FAILED_PLAYLIST_ID=  # Optional: playlist for permanently failed videos

# Optional: Download configuration
DOWNLOAD_PATH=./downloads
POLL_INTERVAL=5
DOWNLOAD_MODE=video  # Options: 'video' (full video) or 'audio' (audio-only, original format)

# Optional: Download retry configuration (exponential backoff)
DOWNLOAD_RETRY_DELAY=60  # Initial retry delay in seconds (default: 60), doubles each retry
DOWNLOAD_RETRY_MAX_BACKOFF=3600  # Max backoff time in seconds (default: 3600 = 1 hour)
DOWNLOAD_FAILURE_THRESHOLD=10  # Attempts before moving to failed playlist (default: 10)

# Optional: Email notifications for permanently failed videos
EMAIL_ENABLED=false  # Set to 'true' to enable email notifications
EMAIL_RECIPIENTS=  # Comma-separated email addresses (e.g., user1@example.com,user2@example.com)
EMAIL_SMTP_HOST=smtp.gmail.com  # SMTP server hostname
EMAIL_SMTP_PORT=587  # SMTP server port (587 for TLS, 465 for SSL)
EMAIL_SMTP_USERNAME=  # SMTP username (usually your email address)
EMAIL_SMTP_PASSWORD=  # SMTP password (use app-specific password for Gmail)
EMAIL_FROM=  # From address (defaults to EMAIL_SMTP_USERNAME if not set)

# Optional: YouTube cookies for bot detection bypass (use ONE method)
COOKIES_FILE=  # Method 1: Path to cookies.txt file
# COOKIES_CONTENT=  # Method 2: Full cookies.txt content as string (preferred for Kubernetes secrets)

# Optional: Prometheus metrics
METRICS_PORT=8080
DAILY_QUOTA_LIMIT=10000  # YouTube API daily quota limit

# Optional: Logging configuration
LOG_LEVEL=INFO  # Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_FILE=/tmp/playlist_manager.log  # Log file path
